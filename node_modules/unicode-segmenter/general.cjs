"use strict";

exports.isAlphabetic = isAlphabetic;
exports.isAlphanumeric = isAlphanumeric;
exports.isLetter = isLetter;
exports.isNumeric = isNumeric;
var _core = require("./core.cjs");
var _general_data = require("./_general_data.cjs");
// @ts-check

const [LETTER_S, LETTER_E] = _general_data.letter_table;
const [ALPHABETIC_S, ALPHABETIC_E] = _general_data.alphabetic_table;
const [NUMERIC_S, NUMERIC_E] = _general_data.numeric_table;

/**
 * Check if the given code point is included in Unicode \\p{L} general property
 *
 * @param {number} cp
 * @return boolean
 */
function isLetter(cp) {
  return (0, _core.findUnicodeRangeCategory)(cp, LETTER_S, LETTER_E) !== 0;
}

/**
 * Check if the given code point is included in Unicode \\p{Alphabetic} dervied property
 *
 * @param {number} cp
 * @return boolean
 */
function isAlphabetic(cp) {
  return (0, _core.findUnicodeRangeCategory)(cp, ALPHABETIC_S, ALPHABETIC_E) !== 0;
}

/**
 * Check if the given code point is included in Unicode \\p{N} general property
 *
 * @param {number} cp
 * @return boolean true if
 */
function isNumeric(cp) {
  return (0, _core.findUnicodeRangeCategory)(cp, NUMERIC_S, NUMERIC_E) !== 0;
}

/**
 * @param {number} cp
 * @return boolean true
 */
function isAlphanumeric(cp) {
  return isAlphabetic(cp) || isNumeric(cp);
}