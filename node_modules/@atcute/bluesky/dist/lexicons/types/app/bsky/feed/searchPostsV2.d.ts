import * as v from '@atcute/lexicons/validations';
import * as AppBskyFeedDefs from './defs.ts';
declare const _mainSchema: v.XRPCQueryMetadata<v.ObjectSchema<{
    /** Search the full index instead of the recent-post window. */
    allTime: v.OptionalSchema<v.BooleanSchema, undefined>;
    /** Include posts by any of these authors. Handles are resolved to DIDs before searching. */
    authors: v.OptionalSchema<v.ArraySchema<v.FormattedStringSchema<"at-identifier">>, undefined>;
    /** Optional pagination cursor. */
    cursor: v.OptionalSchema<v.StringSchema<string>, undefined>;
    /** Include posts that link to any of these domains. */
    domains: v.OptionalSchema<v.ArraySchema<v.StringSchema<string>>, undefined>;
    /** Include posts that embed any of these AT URIs. */
    embeddedAtUris: v.OptionalSchema<v.ArraySchema<v.FormattedStringSchema<"at-uri">>, undefined>;
    /** Exclude posts by any of these authors. Handles are resolved to DIDs before searching. */
    excludeAuthors: v.OptionalSchema<v.ArraySchema<v.FormattedStringSchema<"at-identifier">>, undefined>;
    /** Exclude posts that link to any of these domains. */
    excludeDomains: v.OptionalSchema<v.ArraySchema<v.StringSchema<string>>, undefined>;
    /** Exclude posts that embed any of these AT URIs. */
    excludeEmbeddedAtUris: v.OptionalSchema<v.ArraySchema<v.FormattedStringSchema<"at-uri">>, undefined>;
    /** Exclude posts tagged with any of these hashtags. Do not include the hash (#) prefix. */
    excludeHashtags: v.OptionalSchema<v.ArraySchema<v.SchemaWithConstraint<v.StringSchema<string>, readonly [v.StringLengthConstraint<0, 640>, v.StringGraphemesConstraint<0, 64>]>>, undefined>;
    /** Exclude posts whose language matches any of these language codes. */
    excludeLanguages: v.OptionalSchema<v.ArraySchema<v.FormattedStringSchema<"language">>, undefined>;
    /** Exclude posts that mention any of these accounts. Handles are resolved to DIDs before searching. */
    excludeMentions: v.OptionalSchema<v.ArraySchema<v.FormattedStringSchema<"at-identifier">>, undefined>;
    /** Exclude replies from results. Mutually exclusive with repliesOnly. */
    excludeReplies: v.OptionalSchema<v.BooleanSchema, undefined>;
    /** Exclude posts that link to any of these URLs. */
    excludeUrls: v.OptionalSchema<v.ArraySchema<v.FormattedStringSchema<"uri">>, undefined>;
    /** Include only posts from accounts followed by the viewer. */
    following: v.OptionalSchema<v.BooleanSchema, undefined>;
    /** Include only posts with media. */
    hasMedia: v.OptionalSchema<v.BooleanSchema, undefined>;
    /** Include only posts with video. */
    hasVideo: v.OptionalSchema<v.BooleanSchema, undefined>;
    /** Include posts tagged with any of these hashtags. Do not include the hash (#) prefix. */
    hashtags: v.OptionalSchema<v.ArraySchema<v.SchemaWithConstraint<v.StringSchema<string>, readonly [v.StringLengthConstraint<0, 640>, v.StringGraphemesConstraint<0, 64>]>>, undefined>;
    /** Include posts whose language matches any of these language codes. */
    languages: v.OptionalSchema<v.ArraySchema<v.FormattedStringSchema<"language">>, undefined>;
    /**
     * Maximum number of results to return.
     *
     * @default 25
     * @minimum 1
     * @maximum 100
     */
    limit: v.OptionalSchema<v.SchemaWithConstraint<v.IntegerSchema, readonly [v.IntegerRangeConstraint<1, 100>]>, 25>;
    /** Include posts that mention any of these accounts. Handles are resolved to DIDs before searching. */
    mentions: v.OptionalSchema<v.ArraySchema<v.FormattedStringSchema<"at-identifier">>, undefined>;
    /** Search query string. A query or at least one filter is required. */
    query: v.OptionalSchema<v.StringSchema<string>, undefined>;
    /** Language analyzer hint for the query text. If unset, the server auto-detects when possible. */
    queryLanguage: v.OptionalSchema<v.StringSchema<"ar" | "ja" | "ko" | "th" | "zh" | (string & {})>, undefined>;
    /** Include only replies. Mutually exclusive with excludeReplies. */
    repliesOnly: v.OptionalSchema<v.BooleanSchema, undefined>;
    /** Include only direct replies to this parent post URI. */
    replyParentUri: v.OptionalSchema<v.FormattedStringSchema<"at-uri">, undefined>;
    /** Include posts indexed at or after this timestamp. Can be a datetime, or just an ISO date (YYYY-MM-DD). */
    since: v.OptionalSchema<v.StringSchema<string>, undefined>;
    /** Ranking order for results. 'recent' sorts by recency; 'top' uses search ranking. */
    sort: v.OptionalSchema<v.StringSchema<"recent" | "top" | (string & {})>, undefined>;
    /** Include only posts in the thread rooted at this post URI. */
    threadRootUri: v.OptionalSchema<v.FormattedStringSchema<"at-uri">, undefined>;
    /**
     * Include posts indexed before this timestamp. Defaults to the current time. Can be a datetime, or just
     * an ISO date (YYYY-MM-DD).
     */
    until: v.OptionalSchema<v.StringSchema<string>, undefined>;
    /** Include posts that link to any of these URLs. */
    urls: v.OptionalSchema<v.ArraySchema<v.FormattedStringSchema<"uri">>, undefined>;
}>, {
    type: "lex";
    schema: v.ObjectSchema<{
        /** Cursor for the next page of results. */
        cursor: v.OptionalSchema<v.StringSchema<string>, undefined>;
        /** Query languages detected for CJK, Thai, or Arabic text. Empty or omitted for other scripts. */
        detectedQueryLanguages: v.OptionalSchema<v.ArraySchema<v.StringSchema<"ar" | "ja" | "ko" | "th" | "zh" | (string & {})>>, undefined>;
        /** Estimated total number of matching hits. May be rounded or truncated. */
        hitsTotal: v.OptionalSchema<v.IntegerSchema, undefined>;
        /** Hydrated views of matching posts. */
        readonly posts: v.ArraySchema<AppBskyFeedDefs.postViewSchema>;
    }>;
}, "app.bsky.feed.searchPostsV2">;
type main$schematype = typeof _mainSchema;
export interface mainSchema extends main$schematype {
}
export declare const mainSchema: mainSchema;
export interface $params extends v.InferInput<mainSchema['params']> {
}
export interface $output extends v.InferXRPCBodyInput<mainSchema['output']> {
}
declare module '@atcute/lexicons/ambient' {
    interface XRPCQueries {
        'app.bsky.feed.searchPostsV2': mainSchema;
    }
}
export {};
